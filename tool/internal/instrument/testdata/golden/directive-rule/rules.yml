span_directive:
  where:
    target: main
    directive: "otelc:span"
  do:
    expand_directive:
      template: |-
        println("span start: {{FuncName}}")
        defer println("span end: {{FuncName}}")
