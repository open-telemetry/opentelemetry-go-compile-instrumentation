wrap_sizeof_with_strings:
  where:
    target: main
    function_call: unsafe.Sizeof
  do:
    wrap_call:
      template: "(func() uintptr { fmt.Println(\"Wrapped!\"); return {{ . }} })()"
  imports:
    fmt: "fmt"
